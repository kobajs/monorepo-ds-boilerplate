export interface IShape {
  borderRadius: {
    small: number
    medium: number
    large: number
  }
}

const shape: IShape = {
  borderRadius: {
    small: 4,
    medium: 8,
    large: 12,
  }
}

export default shape